# Skill Registry

- `mobile-agent.md` applies to this repository: `mobile`.
- `web-agent.md` applies to the sibling admin repository: `../admin-panel-budget`.
- `api-agent.md` applies to the sibling backend repository: `../Back-end-budget-app`.
- These files are enforced by `agents.md` as hard environment constraints.
